# THIS FILE IS MANAGED BY PUPPET.

service check_mk
{
	type           = UNLISTED
	port           = <%= @port %>
	socket_type    = stream
	protocol       = tcp
	wait           = no
	user           = <%= @user %>
	server         = <%= @server %>
<% if @only_from -%>
	only_from      = 127.0.0.1 <%= @only_from %>
<% end -%>
	log_on_success =
	disable        = no
}
